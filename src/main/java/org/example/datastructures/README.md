# 数据结构专题

常用数据结构的 Java 实现，理解底层原理而非仅调用 JDK 集合 API。

---

## 学习建议

数据结构是算法和集合框架的基础。建议学习顺序：

```
数组 / 链表 → 栈 / 队列 → 树（二叉树/BST/红黑树）→ 堆 → 哈希表 → 图
```

理解每种结构的「增删查时间复杂度」和「适用场景」，再回头看 JDK 中 `ArrayList` / `LinkedList` / `HashMap` / `TreeMap` 的设计会豁然开朗。

